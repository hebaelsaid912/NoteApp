<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.NoteAPP" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.NoteAPP.search" parent="Theme.NoteAPP">
        <!-- textColor-->
        <item name="android:editTextColor">@color/purple_500</item>
        <!-- textColor-->
        <item name="android:textColorHint">@color/gray</item>
        <!-- iconColor-->
        <item name="android:iconTint">@color/white</item>
        <!-- iconColor-->
        <item name="android:hint">@string/search</item>

    </style>
</resources>